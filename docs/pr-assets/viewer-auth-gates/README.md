# Viewer authorization gate evidence

These screenshots were captured from the plugin commit under review.

- `telegram-desktop-owner-auth.png` shows a code-protected share rendered in
  Telegram Desktop for the assigned owner without an access-code form.
- `browser-link-only.png` shows the same local app rendered from a link-only
  share in a fresh ordinary browser tab without an access-code form.

The test page contains only synthetic content. The Telegram image is cropped
to the native Mini App window and contains no private chats, session URLs, or
access codes.
